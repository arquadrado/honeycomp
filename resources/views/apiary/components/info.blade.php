<template id="info">
    <label for="name">Nome</label>
    <div class="name">@{{ name }}</div>
    <label for="name">Localização</label>
    <div class="location">@{{ location }}</div>
    <label for="name">Flora dominante</label>
    <div class="dominant-flora">@{{ flora }}</div>
</template>